Fix environment variable names in planning and expense service deployment manifests.
files:
- k8s/planning-service/deployment.yaml
- k8s/expense-service/deployment.yaml
planning-service/deployment.yaml:
add a second env var named PLANNING_DATABASE_URL sourced from svp-secret key DATABASE_URL_PLANNING.
keep the existing DATABASE_URL env var as well.
expense-service/deployment.yaml:
add a second env var named EXPENSE_DATABASE_URL sourced from svp-secret key DATABASE_URL_EXPENSE.
keep the existing DATABASE_URL env var as well.
imports / dependencies:
planning_service/alembic/[env.py](http://env.py) reads os.getenv("PLANNING_DATABASE_URL").
expense_service/alembic/[env.py](http://env.py) reads os.getenv("EXPENSE_DATABASE_URL").
customer_user_service/alembic/[env.py](http://env.py) reads os.getenv("DATABASE_URL"), already correct.
standards:
follow kubernetes best practices.
use resource requests and limits on all containers.
add liveness and readiness probes where appropriate.